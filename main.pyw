#!/usr/bin/env python3
"""Windows no-console launcher for the GUI.

The application code lives in main.py; keep this file tiny so the .py and .pyw
entry points cannot drift apart.
"""

from main import main


if __name__ == "__main__":
    main()
